# Integration Testing a Shell Script

A simple example of integration testing a shell script. We can assert the output and the exit code.

For unit testing, our script must be split into functions and sourced.
